import React, {useState} from "react";
import ReactDOM from "react-dom";

import "./style.css";

const App = () => {
    return (
        <div className="App">
            <h1>RTL</h1>
        </div>
    )
};

ReactDOM.render(
    <App/>,
    document.getElementById("root"),
);
